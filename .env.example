# Copy to .env and fill in. Never commit .env.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-haiku-4-5-20251001

# show stack traces in error responses — local dev only, never in production
APP_DEBUG=true
